apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "demoapp2025"
  namespace: "client-apps"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: "demoapp2025"
  template:
    metadata:
      labels:
        app: "demoapp2025"
    spec:
      containers:
      - name: "demoapp2025"
        image: "registry.gitlab.com/home-server5364202/demo:latest"
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: "pull-secret-demoapp2025"
---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "demoapp2025-svc"
  namespace: "client-apps"
spec:
  type: "ClusterIP"
  selector:
    app: "demoapp2025"
  ports:
  - port: 80
    targetPort: 8010
---
apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "demoapp2025-ingress"
  namespace: "client-apps"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: "/"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  rules:
  - host: "demoapp2025-8hengy.naboulsiamine.com"
    http:
      paths:
      - path: "/"
        pathType: "Prefix"
        backend:
          service:
            name: "demoapp2025-svc"
            port:
              number: 80
