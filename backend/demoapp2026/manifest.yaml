apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "demoapp2026"
  namespace: "client-apps"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: "demoapp2026"
  template:
    metadata:
      labels:
        app: "demoapp2026"
    spec:
      containers:
      - name: "demoapp2026"
        image: "registry.gitlab.com/home-server5364202/demo:latest"
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: "pull-secret-demoapp2026"
---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "demoapp2026-svc"
  namespace: "client-apps"
spec:
  type: "ClusterIP"
  selector:
    app: "demoapp2026"
  ports:
  - port: 80
    targetPort: 8010
---
apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "demoapp2026-ingress"
  namespace: "client-apps"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: "/"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  rules:
  - host: "demoapp2026-c22s54.naboulsiamine.com"
    http:
      paths:
      - path: "/"
        pathType: "Prefix"
        backend:
          service:
            name: "demoapp2026-svc"
            port:
              number: 80
