apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "demoapp2"
  namespace: "client-apps"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: "demoapp2"
  template:
    metadata:
      labels:
        app: "demoapp2"
    spec:
      containers:
      - name: "demoapp2"
        image: "registry.gitlab.com/home-server5364202/demo:latest"
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: "pull-secret-demoapp2"
